# npm modules versions

Always use fixed versions for any installations, e.g. `"package-name": "1.2.3"`.
If there is version defined in bun catalog inside root `package.json` — use `catalog:` as value.
